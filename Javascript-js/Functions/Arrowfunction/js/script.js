const square = (x) => x * x;

const res = square(5)

console.log(res)

// 1. return the sum of two numbers
const add = (a,b) => a+b

// 2. return the difference of two numbers
const sub = (a,b) => a-b

// 3. return the square of numbers
const squr = (a) => a*a

// 4. return the cude of numbers
const cube = (a) => a*a*a

// 5. check for even
const isEven = (a) => a%2==0

// 6. check for odd
const isOdd = (a) => a%2!=0

// 7. return the max of two numbers
const chkmax = (a,b) => Math.max(a,b)

// 8. return the length of string
const strlen = (str) => str.length

// 9. return the first char of string
const first = (str) => str[0]

// 10. return the last char of string
const last = (str) => str[str.length-1]

// 11. concat strings
const concatstr = (str1,str2) => str1+str2